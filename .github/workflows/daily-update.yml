# 평일 20:30 KST — 종목 동기화 + KRX 수집 (Supabase 직접 반영)
# KOSPI / KOSDAQ 병렬 수집, PER/PBR 생략
#
# GitHub Secrets:
#   SUPABASE_DB_PASSWORD, KRX_ID, KRX_PW

name: Daily data update

on:
  schedule:
    # 20:30 KST = 11:30 UTC, 월~금
    - cron: "30 11 * * 1-5"
  workflow_dispatch:

jobs:
  prepare:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: scripts/requirements.txt

      - name: Install Python dependencies
        run: pip install -r scripts/requirements.txt

      - name: Configure DATABASE_URL
        env:
          SUPABASE_DB_PASSWORD: ${{ secrets.SUPABASE_DB_PASSWORD }}
        run: |
          python <<'PY'
          import os
          from urllib.parse import quote

          password = os.environ.get("SUPABASE_DB_PASSWORD", "").strip()
          if not password:
              raise SystemExit("SUPABASE_DB_PASSWORD secret이 없습니다.")

          encoded = quote(password, safe="")
          ref = "pcdhshflarandsdwuddb"
          host = "aws-1-ap-northeast-2.pooler.supabase.com"
          url = (
              f"postgresql://postgres.{ref}:{encoded}@{host}:5432/postgres"
              "?options=-c%20statement_timeout%3D0"
          )

          github_env = os.environ["GITHUB_ENV"]
          with open(github_env, "a", encoding="utf-8") as f:
              f.write(f"DATABASE_URL={url}\n")
          PY

      - name: Ensure DB schema
        run: python scripts/ensure_schema.py

      - name: Sync stock master
        env:
          KRX_ID: ${{ secrets.KRX_ID }}
          KRX_PW: ${{ secrets.KRX_PW }}
        run: python scripts/sync_stocks.py --market ALL

  ingest:
    needs: prepare
    runs-on: ubuntu-latest
    timeout-minutes: 180
    strategy:
      fail-fast: false
      matrix:
        market: [KOSPI, KOSDAQ]
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: scripts/requirements.txt

      - name: Install Python dependencies
        run: pip install -r scripts/requirements.txt

      - name: Configure DATABASE_URL
        env:
          SUPABASE_DB_PASSWORD: ${{ secrets.SUPABASE_DB_PASSWORD }}
        run: |
          python <<'PY'
          import os
          from urllib.parse import quote

          password = os.environ.get("SUPABASE_DB_PASSWORD", "").strip()
          if not password:
              raise SystemExit("SUPABASE_DB_PASSWORD secret이 없습니다.")

          encoded = quote(password, safe="")
          ref = "pcdhshflarandsdwuddb"
          host = "aws-1-ap-northeast-2.pooler.supabase.com"
          url = (
              f"postgresql://postgres.{ref}:{encoded}@{host}:5432/postgres"
              "?options=-c%20statement_timeout%3D0"
          )

          github_env = os.environ["GITHUB_ENV"]
          with open(github_env, "a", encoding="utf-8") as f:
              f.write(f"DATABASE_URL={url}\n")
          PY

      - name: Ingest ${{ matrix.market }}
        env:
          KRX_ID: ${{ secrets.KRX_ID }}
          KRX_PW: ${{ secrets.KRX_PW }}
        run: |
          python scripts/ingest.py \
            --market ${{ matrix.market }} \
            --days 5 \
            --delay 0.25 \
            --skip-fundamentals
